//export interface PeriodicElement {   // Escrito asi en el componente listar-articulos.ts
export interface PeriodicElement {
    name: string;
    position: number;
    weight: number;
    symbol: string;
}
